import { userTypeDefs } from "./typedef";
import { queries } from "./queries";
import { mutations } from "./mutations";
import { resolvers } from "./resolvers";

export const User = {userTypeDefs,queries,mutations,resolvers}